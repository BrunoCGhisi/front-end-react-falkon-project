import {Paper, Typography} from "@mui/material";

export default function Produtos() {
    return (
        <Paper sx={{ p: 3 }}>
            <Typography variant="h4">
                Produtos
            </Typography>
        </Paper>
    );
}
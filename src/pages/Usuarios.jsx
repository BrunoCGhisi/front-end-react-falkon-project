import {Paper, Typography} from "@mui/material";

export default function Usuarios() {
    return (
        <Paper sx={{ p: 3 }}>
            <Typography variant="h4">
                Usuarios
            </Typography>
        </Paper>
    );
}